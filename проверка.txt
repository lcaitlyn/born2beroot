


////////////////////////////
//	хэш сумма
//

cd ~/goinfre/born2beroot
shasum born2beroot.vdi




////////////////////////////
//	mandatory part
//

Debian - Имеет наибольшее среди всех дистрибутивов хранилище пакетов(программ и библиотек).
различия:
1) Отличаются поддерживаемые архитектуры.
2) Управление пакетами. Используют разные форматы пакетов в качестве диспетчера пакетов.
3) Поддерживают разные файловые системы. CentOS официально не поддерживает некоторые файловые системы, которые предлагает Debian.

Что такое виртуальная машина. Позволяет установить внутри себя другую операционную систему. Например, можно протестировать линукс, не устанавливая его, 
он будет работать внутри виртуальной машины. основная система - хост система; система внутри виртуальной машины - гостевая система. 
Какова его цель?
О: Виртуальные машины могут быть развернуты для удовлетворения различных уровней потребностей в вычислительной мощности, для запуска программного обеспечения, требующего другого
операционной системы или для тестирования приложений в безопасной изолированной среде.

Разница между aptitude и apt. Оба - менеджеры пакетов, которые могут их устанавливать, удалять, делать по ним поиск и т.д 
 Aptitude - это менеджер пакетов высокого уровня, более функциональный, имеет текстовый интерактивный интерфейс,  а APT - менеджер пакетов более низкого уровня, который может использоваться другими 
менеджеры пакетов более высокого уровня, ограничен командной строкой.

Что такое AppArmor?
AppArmor («Application Armor») - это модуль безопасности ядра Linux, который позволяет системному администратору ограничивать возможности каждой программы.




////////////////////////////
//	simple setup
//

sudo service ufw status
sudo service ssh status
sudo service --status-all

head -n 2 /etc/os-release	просмотреть версию linux
uname -v			просмотреть версию linux

Что такое SSH?
SSH, также известный как Secure Shell или Secure Socket Shell, представляет собой сетевой протокол, который дает пользователям, в частности, системным 
администраторы, безопасный способ доступа к компьютеру по незащищенной сети.

UFW (Uncomplicated Firewall) - является самым простым и довольно популярным инструментарием командной строки для настройки и управления брандмауэром в дистрибутивах Ubuntu и Debian.




////////////////////////////
//	user
//

getent group		getent собирает элементы из административных баз данных, используя указанные ключи поиска

getent group sudo	посмотреть группу sudo
getent group user42	посмотреть группу user42

adduser <name> (или useradd <name>)

Разница между adduser и useradd. Обе команды создают нового пользователя в системе. Но они очень сильно отличаются. Adduser - утилита для добавления пользователей и групп с систему. 
Useradd- это низкоуровневая утилита для добавления пользователей.

vim /etc/login.defs
PASS_MAX_DAYS   30 
PASS_MIN_DAYS   2
PASS_WARN_AGE   7

sudo chage -l lcaitlyn	поддтверждение срока действия нового пароля для пользователя

sudo apt install libpam-pwquality
vim /etc/pam.d/common-password
retry = 3		\\ запросить пользователя 3 раза ввести пароль, прежде чем вернуть ошибку;
minlen = 10		\\ длина пароля не может быть меньше, чем этот параметр;
maxrepeat = 3		\\ разрешить максимум 3 повторяющихся символа;
ucredit = -1		\\ требуется хотя бы один заглавный символ;
lcredit = -1		\\ должен иметь хотя бы один символ нижнего регистра;
dcredit = -1		\\ должен иметь хотя бы одну цифру;
difok = 7		\\ разрешенные повторения строго пароля;
reject_username	\\ запретить username в качестве пароля;
enforce_for_root	\\ реализовать политику в корневом каталоге;

passwd			изменить пароль


sudo addgroup evaluating		\\ добавить группу evaluating
sudo adduser <user> evaluating	\\ добавить пользователя в группу evaluating




////////////////////////////
//	Hostname and partitions
//

hostname			узнать имя хоста
sudo vim /etc/hostname	изменить имя хоста

lsblk				просмотреть разделы для этой виртуальной машины 
df				

LVM — это дополнительный слой абстракции от железа, позволяющий собрать кучи разнородных дисков в один, и затем снова разбить этот один именно так как нам хочется.




////////////////////////////
//	Hostname and partitions
//

sudo apt install sudo	проверяем что судо установлена верно
sudo adduser <user> sudo	добавляет пользователя в sudo

sudo (substitute user and do) Sudo означает либо « Заместитель пользователя делает », либо « Суперпользователь делает » (в зависимости от того, как вы хотите посмотреть на это). То, что делает sudo, невероятно важно и важно для многих дистрибутивов Linux. По сути, sudo позволяет пользователю запускать программу как другой пользователь (чаще всего пользователь root)

sudo ls /var/log/sudo	

////////////////////////////
//	UFW
//

sudo apt install ufw
sudo ufw status

UFW (Uncomplicated Firewall) - является самым простым и довольно популярным инструментарием командной строки для настройки и управления брандмауэром в дистрибутивах Ubuntu и Debian.

sudo ufw allow 8080
sudo ufw status numbered
sudo ufw delete <number>




////////////////////////////
//	SSH
//

sudo apt install openssh-server
sudo service ssh status

Что такое SSH?
SSH, также известный как Secure Shell или Secure Socket Shell, представляет собой сетевой протокол, который дает пользователям, в частности, системным 
администраторы, безопасный способ доступа к компьютеру по незащищенной сети.

sudo vim /etc/ssh/sshd_config	(по умолчанию стоит Port 22)
ssh <name>@localhost -p 4242	подключится по ssh




////////////////////////////
//	Script monitoring
//

uname		выдает информацию о системе
uname -a	выдает всю информацию о системе

grep		находит на вводе строки, отвечающие заданному регулярному выражению, и выводит их

wc		выводит к-во строк, к-во слов,  к-во символов
wc -l		выводит к-во строк

free		выводит информацию об использовании оперативной памяти в Кибибитах (1 кибибайт = 1000 байт)
free --mega 	выводит информацию об использовании оперативной памяти в Мегабайтах

awk		инструмент для обработки и фильтрации текста

df		просмотреть разделы для этой виртуальной машины (аналог lsblk)

mpstat		утилита для просмотра производительности (apt install)

who		получения информации о пользователях, которые подключены к системе
who -b		time of last system boot

hostname -I	отображение всех сетевых адресов хоста
ip link show	используется для отображения и изменения сетевых интерфейсов

journalctl	инструмент для анализа логов



cron — классический демон, использующийся для периодического выполнения заданий в определённое время. Регулярные действия описываются инструкциями, помещенными в файлы crontab и в специальные каталоги.

sudo crontab -e




////////////////////////////
//	Bonus
//

Lighted	веб-сервер, разрабатываемый с расчётом на скорость и защищённость, а также соответствие стандартам.
MariaDB	серверная программа для хранения и получения данных с помощью SQL-запросов.
PHP		скриптовый язык общего назначения, интенсивно применяемый для разработки веб-приложений.
FTP-server	это сервер, работающий по протоколу File Transfer Protocol и предназначенный для обмена файлами через Интернет или локальную компьютерную сеть.


















